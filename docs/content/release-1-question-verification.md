# Release 1 question verification (launch content)

Provenance for the 40 questions added for launch (PR A of the launch-content
work). Every question was authored by fetching the linked MDN page at
authoring time (2026-07-27) and grounding the correct answer, distractors,
hints, and explanation in the fetched content — never in memory. The quoted
claim below is the sentence (or sentences) from the page that supports the
correct answer. The 10 `starter-*` questions predate this document.

| id                        | MDN source                                                                                          | Supporting claim (quoted from the page)                                                                                                                                                                                                             |
| ------------------------- | --------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `launch-html-01`          | <https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button>                       | type attribute: "submit … This is the default if the attribute is not specified for buttons associated with a <form>, or if the attribute is an empty or invalid value."                                                                            |
| `launch-html-02`          | <https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/template>                     | "By default, the element's content is not rendered." + content property "is read-only and holds a DocumentFragment that contains the DOM subtree represented by the template."                                                                      |
| `launch-html-03`          | <https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/details>                      | "The details are shown when this attribute exists, or hidden when this attribute is absent." + "You have to remove this attribute entirely to make the details hidden. open=\"false\" makes the details visible because this attribute is Boolean." |
| `launch-html-04`          | <https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/img>                          | "Defers loading the image until it reaches a calculated distance from the viewport, as defined by the browser." / eager: "Loads the image immediately … (this is the default value)."                                                               |
| `launch-html-05`          | <https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/datalist>                     | "we give it a unique identifier in the id attribute, and then add the list attribute to the <input> element with the same identifier as value." + "The control can still accept any value that passes validation."                                  |
| `launch-html-06`          | <https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Global_attributes/inputmode>           | "the inputmode attribute doesn't cause any validity requirements to be enforced on input" / numeric: "Numeric input keyboard, but only requires the digits 0–9."                                                                                    |
| `launch-css-01`           | <https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Selectors/:where>                       | ":where() always has 0 specificity, whereas :is() takes on the specificity of the most specific selector in its arguments."                                                                                                                         |
| `launch-css-02`           | <https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/position>                    | "At least one inset property … needs to be set to a non-auto value for the axis on which the element needs to be made sticky. If both inset properties for an axis are set to auto, on that axis the sticky value will behave as relative."         |
| `launch-css-03`           | <https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/aspect-ratio>                | "At least one of the box's sizes needs to be automatic in order for aspect-ratio to have any effect. If neither the width nor height is an automatic size, then the provided aspect ratio has no effect."                                           |
| `launch-css-04`           | <https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/margin-inline>               | "This property corresponds to the margin-top and margin-bottom, or the margin-right and margin-left properties, depending on the values defined for writing-mode, direction, and text-orientation."                                                 |
| `launch-css-05`           | <https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/minmax>                          | "The minmax() CSS function defines a size range greater than or equal to min and less than or equal to max. It is used with CSS grids."                                                                                                             |
| `launch-css-06`           | <https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/color_value>                     | "The currentColor keyword represents the value of an element's color property."                                                                                                                                                                     |
| `launch-css-07`           | <https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@media/prefers-reduced-motion> | "used to detect if a user has enabled a setting on their device to minimize the amount of non-essential motion."                                                                                                                                    |
| `launch-css-08`           | <https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Selectors/:has>                         | "represents an element if any of the relative selectors that are passed as an argument match at least one element when anchored against this element" — "a way of selecting a parent element or a previous sibling element."                        |
| `launch-css-09`           | <https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Values/length>                          | rem: "Represents the font-size of the root element (typically <html>)." / em: "Represents the calculated font-size of the element."                                                                                                                 |
| `launch-css-10`           | <https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/container-type>              | inline-size: "Establishes a query container for dimensional queries on the inline axis of the container." / size: "…in both the inline and block dimensions."                                                                                       |
| `launch-javascript-01`    | <https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/let>                  | "While inside the TDZ, the variable has not been initialized with a value, and any attempt to access it will result in a ReferenceError."                                                                                                           |
| `launch-javascript-02`    | <https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/all>      | "It rejects when any of the input's promises rejects, with this first rejection reason."                                                                                                                                                            |
| `launch-javascript-03`    | <https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/at>         | "Negative integers count back from the last item in the array."                                                                                                                                                                                     |
| `launch-javascript-04`    | <https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/typeof>                | "null had 0 as type tag, hence the typeof return value 'object'" + the rejected opt-in fix.                                                                                                                                                         |
| `launch-javascript-05`    | <https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Nullish_coalescing>    | "returns its right-hand side operand when its left-hand side operand is null or undefined" vs \|\|: "returns the right-hand side operand if the left operand is any falsy value."                                                                   |
| `launch-javascript-06`    | <https://developer.mozilla.org/en-US/docs/Web/API/Window/queueMicrotask>                            | "will run after the current task has completed its work and when there is no other code waiting to be run before control of the execution context is returned to the browser's event loop."                                                         |
| `launch-javascript-07`    | <https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Map>              | "Any value (both objects and primitive values) may be used as either a key or a value." / Object: "keys … must be either a String or a Symbol."                                                                                                     |
| `launch-javascript-08`    | <https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/flat>       | "The depth level specifying how deep a nested array structure should be flattened. Defaults to 1."                                                                                                                                                  |
| `launch-javascript-09`    | <https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/const>                | "It does not mean the value it holds is immutable — just that the variable identifier cannot be reassigned" + "its properties can be added, updated, or removed."                                                                                   |
| `launch-javascript-10`    | <https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/isArray>    | "Array.isArray() accepts Array objects constructed in another realm — instanceof Array returns false for these because the identity of the Array constructor is different across realms."                                                           |
| `launch-javascript-11`    | <https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON/stringify>   | "they are either omitted (when found in an object) or changed to null (when found in an array)."                                                                                                                                                    |
| `launch-javascript-12`    | <https://developer.mozilla.org/en-US/docs/Web/API/EventTarget/addEventListener>                     | "the listener should be invoked at most once after being added. If true, the listener would be automatically removed when invoked."                                                                                                                 |
| `launch-web-apis-01`      | <https://developer.mozilla.org/en-US/docs/Web/API/AbortSignal>                                      | "When abort() is called, the fetch() promise rejects with a DOMException named AbortError."                                                                                                                                                         |
| `launch-web-apis-02`      | <https://developer.mozilla.org/en-US/docs/Web/API/Window/sessionStorage>                            | "A page session lasts as long as the tab or the browser is open, and survives over page reloads" + "Closing the tab/window ends the session and clears the data."                                                                                   |
| `launch-web-apis-03`      | <https://developer.mozilla.org/en-US/docs/Web/API/Clipboard_API>                                    | "allows users to programmatically read and write … in secure contexts, provided the user has met the criteria outlined in the Security considerations."                                                                                             |
| `launch-web-apis-04`      | <https://developer.mozilla.org/en-US/docs/Web/API/Response/ok>                                      | "a Boolean stating whether the response was successful (status in the range 200-299) or not."                                                                                                                                                       |
| `launch-web-apis-05`      | <https://developer.mozilla.org/en-US/docs/Web/API/URLSearchParams/get>                              | "A string if the given search parameter is found; otherwise, null."                                                                                                                                                                                 |
| `launch-web-apis-06`      | <https://developer.mozilla.org/en-US/docs/Web/API/Window/storage_event>                             | "The event is not fired on the window that made the change." + "fired in all other browsing contexts that are in the same origin."                                                                                                                  |
| `launch-web-apis-07`      | <https://developer.mozilla.org/en-US/docs/Web/API/ResizeObserver>                                   | "reports changes to the dimensions of an Element's content or border box" + "even if they have nothing to do with the viewport."                                                                                                                    |
| `launch-web-apis-08`      | <https://developer.mozilla.org/en-US/docs/Web/API/Service_Worker_API>                               | "Service workers run in a worker context: they therefore have no DOM access and run on a different thread."                                                                                                                                         |
| `launch-accessibility-01` | <https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Reference/Attributes/aria-hidden>  | "hides content from assistive technology but doesn't visually hide anything" + "removes that element and all of its children from the accessibility tree" + focusable warning.                                                                      |
| `launch-accessibility-02` | <https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/img>                          | "Setting this attribute to an empty string (alt=\"\") indicates that this image is not a key part of the content … and that non-visual browsers may omit it from rendering."                                                                        |
| `launch-accessibility-03` | <https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Reference/Attributes/aria-live>    | "don't use the assertive value unless the interruption is imperative."                                                                                                                                                                              |
| `launch-accessibility-04` | <https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Reference/Roles/button_role>       | "separate key event handlers must be added to the element so that the button is be triggered when the Space or Enter key is pressed" [sic] + native buttons "provide keyboard and focus requirements by default."                                   |
