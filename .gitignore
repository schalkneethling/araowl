# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Playwright
test-results/
playwright-report*/
blob-report/
/full-report.json

# env files — only the varlock schema and committed per-env layers (which
# hold 1Password references, never secret values) are tracked
.env
.env.*
!.env.schema
!.env.production

# local tooling state
.tokensave/
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# pnpm store leak guard (containerized VRT runs)
.pnpm-store/
